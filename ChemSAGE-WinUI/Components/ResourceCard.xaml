<UserControl x:Class="ChemSAGE_WinUI.Components.ResourceCard" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="14" RowSpacing="6">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="{Binding Title}" Style="{StaticResource SubtitleTextBlockStyle}" TextWrapping="Wrap" />
        <TextBlock Grid.Row="1" Text="{Binding Description}" Opacity="0.7" TextWrapping="Wrap" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12">
            <TextBlock Text="{Binding Course}" />
            <TextBlock Text="{Binding Semester}" />
            <TextBlock Text="{Binding ResourceType}" />
            <TextBlock Text="{Binding DownloadCount}" />
        </StackPanel>
    </Grid>
</UserControl>
